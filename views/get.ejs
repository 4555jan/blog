<%- include("partials/header"); -%>
<% posts.forEach(function (post) { %>
  <h2><%= post.title %></h2>
  <p><%= post.text.substring(0,100) %>
    <a href="/posts/<%= post.title %>">readmore</a>

</p>  
<% }); %>

